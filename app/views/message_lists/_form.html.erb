<%= form_with(model: message_list, local: true) do |form| %>
  <% if message_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_list.errors.count, "error") %> prohibited this message_list from being saved:</h2>

      <ul>
      <% message_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :message_list_title %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :message_list_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
